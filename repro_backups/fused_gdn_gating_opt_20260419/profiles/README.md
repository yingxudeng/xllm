# Profiles Omitted From Git Backup

This backup intentionally does not include the original heavy profiling outputs
from:

- `/export/home/dengyingxu1/workspace/fused_gdn_gating_opt/profiles`

Reason:

- raw `msprof` databases and trace outputs are large and not suitable for a
  lightweight Git backup commit

If you need the full profiling artifacts, use the original workspace copy.
