include(cc_library)

cc_library(
  NAME
    cache_layout
  HDRS
    cache_layout.h
  SRCS
    cache_layout.cpp
  DEPS
    :common
    glog::glog
    proto::xllm_proto
)

cc_library(
  NAME
    reshard_planner
  HDRS
    reshard_planner.h
  SRCS
    reshard_planner.cpp
  DEPS
    :cache_layout
    :common
    glog::glog
)

cc_library(
  NAME
    push_route
  HDRS
    push_route.h
  SRCS
    push_route.cpp
)

cc_library(
  NAME
    pd_topology_guard
  HDRS
    pd_topology_guard.h
  SRCS
    pd_topology_guard.cpp
  DEPS
    :cache_layout
    :common
    glog::glog
)

cc_library(
  NAME
    kv_transfer_completion
  HDRS
    kv_transfer_completion.h
  SRCS
    kv_transfer_completion.cpp
  DEPS
    glog::glog
    Folly::folly
)

cc_library(
  NAME
    kv_cache_transfer
  HDRS
    kv_cache_transfer.h
    kv_cache_store.h
    mooncake_store_backend.h
    host_transfer/layout.h
    host_transfer/transfer.h
    host_transfer/basic_transfer.h
    host_transfer/compact_transfer.h
    prefetch_result.h
    hierarchy_kv_cache_transfer.h
    $<$<OR:$<BOOL:${USE_NPU}>,$<BOOL:${USE_MLU}>,$<BOOL:${USE_DCU}>>:mooncake_transfer_engine.h>
    $<$<OR:$<BOOL:${USE_NPU}>,$<BOOL:${USE_MLU}>,$<BOOL:${USE_DCU}>>:mooncake_kv_cache_transfer.h>
    $<$<BOOL:${USE_NPU}>:mooncake_weight_transfer.h>
  SRCS
    kv_cache_transfer.cpp
    kv_cache_store.cpp
    mooncake_store_backend.cpp
    host_transfer/layout.cpp
    host_transfer/transfer.cpp
    host_transfer/transfer_utils.cpp
    host_transfer/basic_transfer.cpp
    host_transfer/compact_transfer.cpp
    host_transfer/compact_load_executor.cpp
    host_transfer/compact_offload_executor.cpp
    hierarchy_kv_cache_transfer.cpp
    $<$<OR:$<BOOL:${USE_NPU}>,$<BOOL:${USE_MLU}>,$<BOOL:${USE_DCU}>>:mooncake_transfer_engine.cpp>
    $<$<OR:$<BOOL:${USE_NPU}>,$<BOOL:${USE_MLU}>,$<BOOL:${USE_DCU}>>:mooncake_kv_cache_transfer.cpp>
    $<$<BOOL:${USE_NPU}>:mooncake_weight_transfer.cpp>
  DEPS
    :cache_layout
    :common
    :kv_cache
    :push_route
    :reshard_planner
    :xtensor
    :util
    :platform
    $<$<BOOL:${USE_NPU}>:graph>
    glog::glog
    $<$<BOOL:${USE_NPU}>:c_sec>
    torch
    $<$<BOOL:${USE_NPU}>:torch_npu>
    mooncake_store
    proto::xllm_proto
    $<$<BOOL:${USE_NPU}>:platform_npu>
    $<$<BOOL:${USE_DCU}>:hip::host>
    $<$<BOOL:${USE_MLU}>:mlu_kernels>
)
